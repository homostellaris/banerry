{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "bun",
			"script": "bun typecheck && bun lint && bun unit && bun integration",
			"problemMatcher": [],
			"label": "bun: test",
			"detail": "bun run bun typecheck && bun lint && bun unit && bun integration - package.json",
			"group": {
				"kind": "test",
				"isDefault": true
			}
		}
	]
}
